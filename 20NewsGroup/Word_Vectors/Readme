The Folder contains the necessary files to generate the word embeddings .
Two methods to generate embeddings can been used Word2vec  or Doc2vecC . 
Also two scipts for each method are present to run on multisense or non multisense corpus

Files present : 

Word2Vec.py  			    : Creates word embeddings from non multisense corpus using Word2Vec
Word2Vec_multisense.py 	    : Creates word embeddings from multisense corpus using Word2Vec

go_nonpolysemy_20news.sh    : Creates word embeddings from non multisense corpus using Doc2VecC
go_polysemy_20news_polysemy : Creates word embeddings from multisense corpus using Doc2VecC


1. Word2Vec (gensim) : 
	
	Word2Vec.py : the script accepts 1 arguement , dimension of the word embedding 

	eg . 
	python3 Word2Vec.py 200
	for embeddings for 200 dimension on non polysemy corpus 

	python3 Word2Vec_multisense.py 200
	for embeddings for 200 dimension on polysemy corpus 


2. Doc2VecC : 

	Doc2VecC embeddings can be generated by running "go_polysemy_20news_polysemy.sh"/"go_polysemy_20news.sh" in Doc2VecC folder . 


Both of the scipts will store the embeddings in the folder Word_Vectors
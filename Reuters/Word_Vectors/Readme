The Folder contains the necessary files to generate the word embeddings .
Two methods to generate embeddings have been used 

1. Word2Vec (gensim) :

	eg . 
	Run for normal word embedding : python Word2Vec.py
	Run for multisense word embedding : Word2Vec_multisense.py

	for embeddings for 200 dimension on polysemy corpus 


2. Doc2VecC : 

	Doc2VecC embeddings can be generated by running "go_polysemy_20news_polysemy.sh" in Doc2VecC folder . 


Both of the scipts will store the embeddings in the folder Word_Vectors